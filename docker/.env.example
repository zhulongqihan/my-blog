# ============================
# MyBlog Docker 环境变量配置
# 复制为 .env 并填写真实值
# ============================

# ---- MySQL ----
MYSQL_ROOT_PASSWORD=RootPass2026
MYSQL_DATABASE=blogdb
MYSQL_USER=bloguser
MYSQL_PASSWORD=MyBlog2026

# ---- RabbitMQ ----
RABBITMQ_DEFAULT_USER=admin
RABBITMQ_DEFAULT_PASS=admin123

# ---- JWT（如需覆盖 docker-compose 默认值）----
JWT_SECRET=0vC37MKrX3ncvfxFacfkojXfawfX8VXtOLjZKA2yU7U=
JWT_EXPIRATION=86400000

# ---- 说明 ----
# Redis 密码、数据库连接、RabbitMQ 连接等均已在 docker-compose.yml 中
# 通过 Spring Boot 标准环境变量直接覆盖（如 SPRING_DATASOURCE_URL），
# 无需在此重复配置。只有需要覆盖 docker-compose.yml 默认值时才在此设置。
